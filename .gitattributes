# This repository stores text files with CRLF line endings in the index.
# Keep Git whitespace checks useful without flagging the CR byte itself.
* whitespace=blank-at-eol,blank-at-eof,space-before-tab,cr-at-eol

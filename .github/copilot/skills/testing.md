# hiredis-happ testing skill shim

The canonical testing workflow is now `.agents/skills/testing/SKILL.md`.

Use that standard Agent Skill for CTest, atframe_utils test macros, Windows DLL lookup, Redis integration tests, and sanitizer-backed lifecycle validation. This legacy Copilot path is kept only as a compatibility pointer.

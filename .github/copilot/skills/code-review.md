# hiredis-happ code-review skill shim

The canonical code-review workflow is now `.agents/skills/code-review/SKILL.md`.

Use that standard Agent Skill for C++ review, Redis Cluster routing, hiredis async lifecycle, command ownership, CI risk triage, and unresolved-risk documentation. This legacy Copilot path is kept only as a compatibility pointer.

# hiredis-happ build skill shim

The canonical build workflow is now `.agents/skills/build/SKILL.md`.

Use that standard Agent Skill for CMake configure/build/install, CI, dependency, packaging, and Windows `hiredis.dll` runtime-path work. This legacy Copilot path is kept only as a compatibility pointer.

language: cpp

compiler:
  - gcc

before_script:
  - sudo apt-get install -qq hiredis
  
after_success:
  - bin/owent_utils_test
  
script: 
  - REPO_DIR=$PWD
  - mkdir -p tools && cd tools
  - CMAKE_PREFIX=$PWD/prebuilt/cmake
  - if [ ! -e "$CMAKE_PREFIX/bin/cmake" ]; then
        wget -c "https://github.com/Kitware/CMake/archive/v3.2.2.tar.gz" ;
        tar -axvf v3.2.2.tar.gz  > build_cmake.log 2>&1 ;
        cd CMake-3.2.2 ;
        ./bootstrap --prefix=$CMAKE_PREFIX  >> build_cmake.log 2>&1 ;
        make -j4 install  >> build_cmake.log 2>&1 ;
    fi
  - mkdir -p $REPO_DIR/build && cd $REPO_DIR/build ;
  - $CMAKE_PREFIX/bin/cmake .. ;
  - make -j4 ;

install: true

cache: 
    apt: true
    directories:
    - $CMAKE_PREFIX
    
branches:
  only:
    - master

notifications:
  email: false
  irc:
    template:
      - "%{repository}/%{branch} (%{commit} - %{author}): %{build_url}: %{message}"

env:
  - LANG="zh_CN.UTF-8"

os:
  - linux
  - osx

osx_image: xcode61

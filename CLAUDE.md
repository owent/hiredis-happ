@AGENTS.md

## Claude Code compatibility

- Treat `AGENTS.md` as the project source of truth.
- Use repository skills under `.agents/skills/` when a task matches their descriptions.
- Keep this file as a thin compatibility shim; do not duplicate project rules here.
